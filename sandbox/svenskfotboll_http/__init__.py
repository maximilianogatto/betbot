"""HTTP-only research tools for Svenskfotboll / Swedish FA data.

This package is intentionally isolated under ``sandbox/``.  It documents and
tests the endpoints needed to build a future BetBot ``StatsProvider`` without
touching the production registry yet.
"""

